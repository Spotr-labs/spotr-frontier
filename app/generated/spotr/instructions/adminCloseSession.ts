/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findConfigPda } from "../pdas";
import { SPOTR_MARKETS_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const ADMIN_CLOSE_SESSION_DISCRIMINATOR = new Uint8Array([
  49, 66, 156, 195, 137, 12, 102, 74,
]);

export function getAdminCloseSessionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ADMIN_CLOSE_SESSION_DISCRIMINATOR,
  );
}

export type AdminCloseSessionInstruction<
  TProgram extends string = typeof SPOTR_MARKETS_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountSession extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountSession extends string
        ? WritableAccount<TAccountSession>
        : TAccountSession,
      ...TRemainingAccounts,
    ]
  >;

export type AdminCloseSessionInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type AdminCloseSessionInstructionDataArgs = {};

export function getAdminCloseSessionInstructionDataEncoder(): FixedSizeEncoder<AdminCloseSessionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: ADMIN_CLOSE_SESSION_DISCRIMINATOR }),
  );
}

export function getAdminCloseSessionInstructionDataDecoder(): FixedSizeDecoder<AdminCloseSessionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getAdminCloseSessionInstructionDataCodec(): FixedSizeCodec<
  AdminCloseSessionInstructionDataArgs,
  AdminCloseSessionInstructionData
> {
  return combineCodec(
    getAdminCloseSessionInstructionDataEncoder(),
    getAdminCloseSessionInstructionDataDecoder(),
  );
}

export type AdminCloseSessionAsyncInput<
  TAccountAuthority extends string = string,
  TAccountConfig extends string = string,
  TAccountSession extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  config?: Address<TAccountConfig>;
  session: Address<TAccountSession>;
};

export async function getAdminCloseSessionInstructionAsync<
  TAccountAuthority extends string,
  TAccountConfig extends string,
  TAccountSession extends string,
  TProgramAddress extends Address = typeof SPOTR_MARKETS_PROGRAM_ADDRESS,
>(
  input: AdminCloseSessionAsyncInput<
    TAccountAuthority,
    TAccountConfig,
    TAccountSession
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AdminCloseSessionInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig,
    TAccountSession
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SPOTR_MARKETS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    session: { value: input.session ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.session),
    ],
    data: getAdminCloseSessionInstructionDataEncoder().encode({}),
    programAddress,
  } as AdminCloseSessionInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig,
    TAccountSession
  >);
}

export type AdminCloseSessionInput<
  TAccountAuthority extends string = string,
  TAccountConfig extends string = string,
  TAccountSession extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  config: Address<TAccountConfig>;
  session: Address<TAccountSession>;
};

export function getAdminCloseSessionInstruction<
  TAccountAuthority extends string,
  TAccountConfig extends string,
  TAccountSession extends string,
  TProgramAddress extends Address = typeof SPOTR_MARKETS_PROGRAM_ADDRESS,
>(
  input: AdminCloseSessionInput<
    TAccountAuthority,
    TAccountConfig,
    TAccountSession
  >,
  config?: { programAddress?: TProgramAddress },
): AdminCloseSessionInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountConfig,
  TAccountSession
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SPOTR_MARKETS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    session: { value: input.session ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.session),
    ],
    data: getAdminCloseSessionInstructionDataEncoder().encode({}),
    programAddress,
  } as AdminCloseSessionInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig,
    TAccountSession
  >);
}

export type ParsedAdminCloseSessionInstruction<
  TProgram extends string = typeof SPOTR_MARKETS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    config: TAccountMetas[1];
    session: TAccountMetas[2];
  };
  data: AdminCloseSessionInstructionData;
};

export function parseAdminCloseSessionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAdminCloseSessionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      config: getNextAccount(),
      session: getNextAccount(),
    },
    data: getAdminCloseSessionInstructionDataDecoder().decode(instruction.data),
  };
}
