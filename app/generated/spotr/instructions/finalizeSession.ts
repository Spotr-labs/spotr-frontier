/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import { SPOTR_MARKETS_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const FINALIZE_SESSION_DISCRIMINATOR = new Uint8Array([
  34, 148, 144, 47, 37, 130, 206, 161,
]);

export function getFinalizeSessionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    FINALIZE_SESSION_DISCRIMINATOR,
  );
}

export type FinalizeSessionInstruction<
  TProgram extends string = typeof SPOTR_MARKETS_PROGRAM_ADDRESS,
  TAccountSession extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSession extends string
        ? WritableAccount<TAccountSession>
        : TAccountSession,
      ...TRemainingAccounts,
    ]
  >;

export type FinalizeSessionInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type FinalizeSessionInstructionDataArgs = {};

export function getFinalizeSessionInstructionDataEncoder(): FixedSizeEncoder<FinalizeSessionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: FINALIZE_SESSION_DISCRIMINATOR }),
  );
}

export function getFinalizeSessionInstructionDataDecoder(): FixedSizeDecoder<FinalizeSessionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getFinalizeSessionInstructionDataCodec(): FixedSizeCodec<
  FinalizeSessionInstructionDataArgs,
  FinalizeSessionInstructionData
> {
  return combineCodec(
    getFinalizeSessionInstructionDataEncoder(),
    getFinalizeSessionInstructionDataDecoder(),
  );
}

export type FinalizeSessionInput<TAccountSession extends string = string> = {
  session: Address<TAccountSession>;
};

export function getFinalizeSessionInstruction<
  TAccountSession extends string,
  TProgramAddress extends Address = typeof SPOTR_MARKETS_PROGRAM_ADDRESS,
>(
  input: FinalizeSessionInput<TAccountSession>,
  config?: { programAddress?: TProgramAddress },
): FinalizeSessionInstruction<TProgramAddress, TAccountSession> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SPOTR_MARKETS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    session: { value: input.session ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta(accounts.session)],
    data: getFinalizeSessionInstructionDataEncoder().encode({}),
    programAddress,
  } as FinalizeSessionInstruction<TProgramAddress, TAccountSession>);
}

export type ParsedFinalizeSessionInstruction<
  TProgram extends string = typeof SPOTR_MARKETS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    session: TAccountMetas[0];
  };
  data: FinalizeSessionInstructionData;
};

export function parseFinalizeSessionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedFinalizeSessionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { session: getNextAccount() },
    data: getFinalizeSessionInstructionDataDecoder().decode(instruction.data),
  };
}
