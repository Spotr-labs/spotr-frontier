/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findConfigPda } from "../pdas";
import { SPOTR_MARKETS_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";
import {
  getSideSelectionDecoder,
  getSideSelectionEncoder,
  type SideSelection,
  type SideSelectionArgs,
} from "../types";

export const RESOLVE_ROUND_DISCRIMINATOR = new Uint8Array([
  165, 114, 237, 158, 1, 36, 70, 254,
]);

export function getResolveRoundDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RESOLVE_ROUND_DISCRIMINATOR,
  );
}

export type ResolveRoundInstruction<
  TProgram extends string = typeof SPOTR_MARKETS_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountSession extends string | AccountMeta<string> = string,
  TAccountRound extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountSession extends string
        ? ReadonlyAccount<TAccountSession>
        : TAccountSession,
      TAccountRound extends string
        ? WritableAccount<TAccountRound>
        : TAccountRound,
      ...TRemainingAccounts,
    ]
  >;

export type ResolveRoundInstructionData = {
  discriminator: ReadonlyUint8Array;
  winningSide: SideSelection;
};

export type ResolveRoundInstructionDataArgs = {
  winningSide: SideSelectionArgs;
};

export function getResolveRoundInstructionDataEncoder(): FixedSizeEncoder<ResolveRoundInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["winningSide", getSideSelectionEncoder()],
    ]),
    (value) => ({ ...value, discriminator: RESOLVE_ROUND_DISCRIMINATOR }),
  );
}

export function getResolveRoundInstructionDataDecoder(): FixedSizeDecoder<ResolveRoundInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["winningSide", getSideSelectionDecoder()],
  ]);
}

export function getResolveRoundInstructionDataCodec(): FixedSizeCodec<
  ResolveRoundInstructionDataArgs,
  ResolveRoundInstructionData
> {
  return combineCodec(
    getResolveRoundInstructionDataEncoder(),
    getResolveRoundInstructionDataDecoder(),
  );
}

export type ResolveRoundAsyncInput<
  TAccountAuthority extends string = string,
  TAccountConfig extends string = string,
  TAccountSession extends string = string,
  TAccountRound extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  config?: Address<TAccountConfig>;
  session: Address<TAccountSession>;
  round: Address<TAccountRound>;
  winningSide: ResolveRoundInstructionDataArgs["winningSide"];
};

export async function getResolveRoundInstructionAsync<
  TAccountAuthority extends string,
  TAccountConfig extends string,
  TAccountSession extends string,
  TAccountRound extends string,
  TProgramAddress extends Address = typeof SPOTR_MARKETS_PROGRAM_ADDRESS,
>(
  input: ResolveRoundAsyncInput<
    TAccountAuthority,
    TAccountConfig,
    TAccountSession,
    TAccountRound
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ResolveRoundInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig,
    TAccountSession,
    TAccountRound
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SPOTR_MARKETS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    session: { value: input.session ?? null, isWritable: false },
    round: { value: input.round ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.session),
      getAccountMeta(accounts.round),
    ],
    data: getResolveRoundInstructionDataEncoder().encode(
      args as ResolveRoundInstructionDataArgs,
    ),
    programAddress,
  } as ResolveRoundInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig,
    TAccountSession,
    TAccountRound
  >);
}

export type ResolveRoundInput<
  TAccountAuthority extends string = string,
  TAccountConfig extends string = string,
  TAccountSession extends string = string,
  TAccountRound extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  config: Address<TAccountConfig>;
  session: Address<TAccountSession>;
  round: Address<TAccountRound>;
  winningSide: ResolveRoundInstructionDataArgs["winningSide"];
};

export function getResolveRoundInstruction<
  TAccountAuthority extends string,
  TAccountConfig extends string,
  TAccountSession extends string,
  TAccountRound extends string,
  TProgramAddress extends Address = typeof SPOTR_MARKETS_PROGRAM_ADDRESS,
>(
  input: ResolveRoundInput<
    TAccountAuthority,
    TAccountConfig,
    TAccountSession,
    TAccountRound
  >,
  config?: { programAddress?: TProgramAddress },
): ResolveRoundInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountConfig,
  TAccountSession,
  TAccountRound
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SPOTR_MARKETS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    session: { value: input.session ?? null, isWritable: false },
    round: { value: input.round ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.session),
      getAccountMeta(accounts.round),
    ],
    data: getResolveRoundInstructionDataEncoder().encode(
      args as ResolveRoundInstructionDataArgs,
    ),
    programAddress,
  } as ResolveRoundInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig,
    TAccountSession,
    TAccountRound
  >);
}

export type ParsedResolveRoundInstruction<
  TProgram extends string = typeof SPOTR_MARKETS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    config: TAccountMetas[1];
    session: TAccountMetas[2];
    round: TAccountMetas[3];
  };
  data: ResolveRoundInstructionData;
};

export function parseResolveRoundInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedResolveRoundInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      config: getNextAccount(),
      session: getNextAccount(),
      round: getNextAccount(),
    },
    data: getResolveRoundInstructionDataDecoder().decode(instruction.data),
  };
}
