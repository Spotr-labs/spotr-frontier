/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findConfigPda } from "../pdas";
import { SPOTR_MARKETS_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const ADMIN_CLOSE_ROUND_DISCRIMINATOR = new Uint8Array([
  66, 210, 165, 132, 223, 67, 241, 200,
]);

export function getAdminCloseRoundDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ADMIN_CLOSE_ROUND_DISCRIMINATOR,
  );
}

export type AdminCloseRoundInstruction<
  TProgram extends string = typeof SPOTR_MARKETS_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountSession extends string | AccountMeta<string> = string,
  TAccountRound extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountSession extends string
        ? WritableAccount<TAccountSession>
        : TAccountSession,
      TAccountRound extends string
        ? WritableAccount<TAccountRound>
        : TAccountRound,
      ...TRemainingAccounts,
    ]
  >;

export type AdminCloseRoundInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type AdminCloseRoundInstructionDataArgs = {};

export function getAdminCloseRoundInstructionDataEncoder(): FixedSizeEncoder<AdminCloseRoundInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: ADMIN_CLOSE_ROUND_DISCRIMINATOR }),
  );
}

export function getAdminCloseRoundInstructionDataDecoder(): FixedSizeDecoder<AdminCloseRoundInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getAdminCloseRoundInstructionDataCodec(): FixedSizeCodec<
  AdminCloseRoundInstructionDataArgs,
  AdminCloseRoundInstructionData
> {
  return combineCodec(
    getAdminCloseRoundInstructionDataEncoder(),
    getAdminCloseRoundInstructionDataDecoder(),
  );
}

export type AdminCloseRoundAsyncInput<
  TAccountAuthority extends string = string,
  TAccountConfig extends string = string,
  TAccountSession extends string = string,
  TAccountRound extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  config?: Address<TAccountConfig>;
  session: Address<TAccountSession>;
  round: Address<TAccountRound>;
};

export async function getAdminCloseRoundInstructionAsync<
  TAccountAuthority extends string,
  TAccountConfig extends string,
  TAccountSession extends string,
  TAccountRound extends string,
  TProgramAddress extends Address = typeof SPOTR_MARKETS_PROGRAM_ADDRESS,
>(
  input: AdminCloseRoundAsyncInput<
    TAccountAuthority,
    TAccountConfig,
    TAccountSession,
    TAccountRound
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AdminCloseRoundInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig,
    TAccountSession,
    TAccountRound
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SPOTR_MARKETS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    session: { value: input.session ?? null, isWritable: true },
    round: { value: input.round ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.session),
      getAccountMeta(accounts.round),
    ],
    data: getAdminCloseRoundInstructionDataEncoder().encode({}),
    programAddress,
  } as AdminCloseRoundInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig,
    TAccountSession,
    TAccountRound
  >);
}

export type AdminCloseRoundInput<
  TAccountAuthority extends string = string,
  TAccountConfig extends string = string,
  TAccountSession extends string = string,
  TAccountRound extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  config: Address<TAccountConfig>;
  session: Address<TAccountSession>;
  round: Address<TAccountRound>;
};

export function getAdminCloseRoundInstruction<
  TAccountAuthority extends string,
  TAccountConfig extends string,
  TAccountSession extends string,
  TAccountRound extends string,
  TProgramAddress extends Address = typeof SPOTR_MARKETS_PROGRAM_ADDRESS,
>(
  input: AdminCloseRoundInput<
    TAccountAuthority,
    TAccountConfig,
    TAccountSession,
    TAccountRound
  >,
  config?: { programAddress?: TProgramAddress },
): AdminCloseRoundInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountConfig,
  TAccountSession,
  TAccountRound
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SPOTR_MARKETS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    session: { value: input.session ?? null, isWritable: true },
    round: { value: input.round ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.session),
      getAccountMeta(accounts.round),
    ],
    data: getAdminCloseRoundInstructionDataEncoder().encode({}),
    programAddress,
  } as AdminCloseRoundInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig,
    TAccountSession,
    TAccountRound
  >);
}

export type ParsedAdminCloseRoundInstruction<
  TProgram extends string = typeof SPOTR_MARKETS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    config: TAccountMetas[1];
    session: TAccountMetas[2];
    round: TAccountMetas[3];
  };
  data: AdminCloseRoundInstructionData;
};

export function parseAdminCloseRoundInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAdminCloseRoundInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      config: getNextAccount(),
      session: getNextAccount(),
      round: getNextAccount(),
    },
    data: getAdminCloseRoundInstructionDataDecoder().decode(instruction.data),
  };
}
